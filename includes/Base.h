#include <GL/glut.h>
#include <bits/stdc++.h>
using namespace std;
#define MAX_WIDTH 800
#define MAX_HEIGHT 600

class Base{
public:
    int width;
    int height;

    
    Base(){
        this->width = MAX_WIDTH;
        this->height = MAX_HEIGHT;
    };
};

